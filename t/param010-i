
define foo {
  number a = 20;
  param number b = a/10;
  number c = b + 1;
}

foo f;

draw { &dump_hash; }

__END__

require 'testutils/dump_hash.pl';

# There should be a failure here because the parameter is 
# not well-defined
