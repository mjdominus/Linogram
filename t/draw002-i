
require "hline";

hline y1, y2, n1, n2;
draw { y1; y2; } 
constraints { 
  y1.start = (0,0);  y1.length = 2;
  y1 + (1, 1) = n1;
  n1 + (1, 1) = y2;
  y2 + (1, 1) = n2;
}

__END__
require 'draw/dummy.pl';
