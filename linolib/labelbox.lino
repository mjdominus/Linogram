
require "box";
require "label";

define labelbox extends box {
        param string text = "";
        label the_label(label=text);
        draw { top; left; bottom; right; the_label; }
        constraints { the_label = c; }
}


